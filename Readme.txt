--Project 1 Exercise project
--Bacteria Data Analysis

To use the following program one must call/execute the main script:
Main_Script.py

The 3 other .py files are separate functions that gets called from the main script.

test.txt is the supplied data file which the project has been built around.
test1.txt is an empty file to test the dataLoad.py's function to handle empty files.
test2.txt is likewise a tester for dataLoad.py that tests against incorrect formatting.
